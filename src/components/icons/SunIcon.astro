---
import type { ComponentProps } from "astro/types";

import { Icon } from "astro-icon/components";

type Props = Omit<ComponentProps<typeof Icon>, "name">;
---

<Icon {...Astro.props} name="sun" />
