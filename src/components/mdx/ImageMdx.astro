---
import { Image } from "astro:assets";

// eslint-disable-next-line
type Props = HTMLImageElement;

const props = Astro.props;
---

<div class="flex flex-row justify-center">
  <Image alt={props.alt} height={600} src={props.src} width={800} />
</div>
